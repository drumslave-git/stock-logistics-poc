/** An item is flagged "low on" when a stock holds fewer than this many units. */
export const LOW_STOCK_THRESHOLD = 10;

/** Mean Earth radius in km, used for haversine distance. */
export const EARTH_RADIUS_KM = 6371;
